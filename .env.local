# 本地 LiteLLM / Claude Code 推理（dev 入口 bun run start 启动前会先读此文件）
#
# ANTHROPIC_BASE_URL 只填到 host + port，不要带 /anthropic 之类后缀：
# Anthropic SDK 内部会自动拼 /v1/messages 等路径，LiteLLM 也暴露在
# http://127.0.0.1:4000/v1/messages。如果这里写成 http://127.0.0.1:4000/anthropic
# ，最终 URL 会变成 http://127.0.0.1:4000/anthropic/v1/messages，LiteLLM 会
# 当成 passthrough 把请求转发到真实的 api.anthropic.com，本地假 token 直接被
# 401，表现为「代理日志看不到请求」。
ANTHROPIC_BASE_URL=http://127.0.0.1:4000
ANTHROPIC_AUTH_TOKEN=sk-litellm-local-dev
